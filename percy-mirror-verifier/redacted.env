# The webhook secret from the Percy project's "Integrations" tab.
PERCY_WEBHOOK_SECRET=[REDACTED]

# The read-only token from the Percy project's "Settings" tab.
PERCY_TOKEN=[REDACTED]

# A GitHub user access token with `public_repo` permission.
GITHUB_ACCESS_TOKEN=[REDACTED]
